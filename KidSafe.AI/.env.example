# Copy to .env and fill in values

# Model backend: stub | hf_pipeline | custom
MODEL_BACKEND=hf_pipeline

# HuggingFace model (only used when MODEL_BACKEND=hf_pipeline)
HF_MODEL_NAME=unitary/toxic-bert
HF_MAX_LENGTH=128

# Toxicity thresholds
FLAGGED_THRESHOLD=0.5
BLOCKED_THRESHOLD=0.8
