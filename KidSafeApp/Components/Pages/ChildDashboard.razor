@page "/child-dashboard"
@inject NavigationManager NavigationManager

<PageTitle>Child Dashboard</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/child/dashboard", replace: true);
    }
}
